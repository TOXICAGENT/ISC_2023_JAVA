public class Remainder extends Calculator
{
    void Remainder()
    {
        Remainder obj = new Remainder();
        obj.getVal();
        int v = a;
        int w = b;
        int remainder = v%w;
        System.out.println(remainder);
    }
}